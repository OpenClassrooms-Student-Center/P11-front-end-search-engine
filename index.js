console.time("index.js");
("use strict");

import { renderRecipes } from "./JS/api.js";

// RECUPERE LA DATA ET HYDRATE LES COMPOSANTS
export const GetRecipesHydrate = (renderRecipes.prototype.getAllRecipes =
   function (recipes) {
   //   console.log(recipes);
     return recipes;
   });
