const rechercheInput = document.querySelector(".rechercheInput");

function rechercheRecettes() {
  si plus de 2 caractères => recherche sinon j'affiche tout.
  si saisie = nom OU ustenciles OU description => affiche les tags des recettes ET les recettes correspondantes
  si rien ne ressort => affiche message erreur
}

function rechercheTags() {
  si tag choisi => affiche boxTag et met à jour la liste des recettes
}
